apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo
  namespace: three-tier
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo
  template:
    metadata:
      labels:
        app: mongo
    spec:
      containers:
      - name: mongo
        image: mongo:6.0
        ports:
        - containerPort: 27017
        env:
        - name: MONGO_INITDB_DATABASE
          value: testdb
        - name: MONGO_INITDB_ROOT_USERNAME
          valueFrom:
            secretKeyRef:
              name: mongo-creds
              key: mongo-username
        - name: MONGO_INITDB_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mongo-creds
              key: mongo-password

        volumeMounts:
        - name: mongo-storage
          mountPath: /data/db

      volumes:
      - name: mongo-storage
        persistentVolumeClaim:
          claimName: mongo-pvc  # This must match the name in your PVC file
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-service
  namespace: three-tier
spec:
  selector:
    app: mongo
  ports:
    - port: 27017
      targetPort: 27017
